# wrapAll() Method

See [Wrapping Methods](./wrapping-methods.md#wrapall) for complete documentation.

## Quick Reference

```javascript
jq(selector).wrapAll(wrappingElement);
```

Wrap all elements together as one group.
